Make sure you are using Python 3 instead of Python 2. To check which version you are using use:

  $ python --version
  
Install required modules:

  $ pip install -U -r requirements.txt
  $ pip --ignore-installed -U setuptools
  
